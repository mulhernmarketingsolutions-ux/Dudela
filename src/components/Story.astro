<section class="story section-light">
  <div class="container story-inner">
    <p class="eyebrow">Why Dudela exists</p>
    <p class="story-lead">
      Nobody teaches dads. Not really. You're expected to figure it out —
      hospital, sleep, your relationship, money, your own head — somehow,
      while pretending you've got it handled.
    </p>
    <p class="story-body">
      Dudela is the roadmap we wish someone had handed us. Built by two dads
      who went through it the hard way, for the dad who wants to walk in
      steady instead of guessing.
    </p>
  </div>
</section>

<section class="stat section-dark">
  <img class="stat-bg" src="/images/golden-field-scenic.jpg" alt="" />
  <div class="stat-scrim"></div>
  <div class="container stat-inner">
    <p class="stat-number">300+</p>
    <p class="stat-caption">dads prepared, one roadmap at a time.</p>
  </div>
</section>

<style>
  .story {
    padding: var(--space-6) 0;
  }
  .story-inner {
    max-width: 760px;
  }
  .story-lead {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 3vw, 2.1rem);
    line-height: 1.35;
    margin: 1rem 0 1.5rem;
  }
  .story-body {
    font-size: 1.05rem;
    color: var(--text-on-light-muted);
    max-width: 58ch;
  }
  .stat {
    position: relative;
    padding: var(--space-6) 0;
    text-align: center;
    overflow: hidden;
  }
  .stat-bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .stat-scrim {
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(18, 24, 15, 0.88) 0%, rgba(18, 24, 15, 0.75) 100%);
  }
  .stat-inner {
    position: relative;
    z-index: 1;
  }
  .stat-number {
    font-family: var(--font-display);
    font-size: clamp(3.5rem, 12vw, 7rem);
    color: var(--amber-500);
    margin: 0;
    line-height: 1;
  }
  .stat-caption {
    font-family: var(--font-label);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-size: 0.85rem;
    color: var(--text-on-dark-muted);
    margin-top: 1rem;
  }
</style>
