# Drop logo files here

From what you shared, save these with the exact filenames below (PNG, transparent background where noted):

| Filename | What it is |
|---|---|
| `logo-badge-green.png` | Circular green badge version (fist bump + DUDELA, on green circle) |
| `logo-full-green.png` | Fist bump + DUDELA wordmark, forest green, transparent background |
| `logo-full-amber.png` | Fist bump + DUDELA wordmark, amber/orange, transparent background |

If you have a fist-bump-icon-only file (no wordmark) or a wordmark-only file, add those too as `logo-icon-only.png` and `logo-wordmark-only.png` — those are the pieces needed to do the flexible Nike-style logo treatment (icon alone, wordmark alone, full mark) discussed in the blueprint.

Once these are in place I'll wire the nav and footer to use the real mark instead of the text wordmark.
